[workspace]
authors = ["1 <1>"]
channels = ["conda-forge"]
name = "testbench"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
python = ">=3.14.6,<3.15"

[pypi-dependencies]
tortoise-orm = { version = ">=1.1.7, <2", extras = ["accel"] }
